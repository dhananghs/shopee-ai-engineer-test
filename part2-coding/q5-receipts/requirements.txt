Django==4.1.9
psycopg2-binary==2.9.6
Pillow==10.0.0
python-dotenv==1.0.0
openai==1.30.1

# openai only asks for "httpx<1", so a fresh install picks up 0.28, which
# removed the "proxies" argument that openai 1.30 still passes to it. every
# call then dies with "Client.__init__() got an unexpected keyword argument
# 'proxies'". pinning the last 0.27 keeps the two in step.
httpx==0.27.2

# only used inside the container. runserver is fine for development but it is
# single threaded and explicitly not meant to face anything real.
gunicorn==21.2.0
